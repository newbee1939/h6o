> **ステータス: 確定版。** 方針を変えるときは本ファイルを直し、PR で履歴を残す。

# h6o — 世界一速い個人ブログ

## 背景
既存のブログサービス（note / Zenn / はてな / Medium）は JS が重く、広告や推薦 UI が載り、**速度を自分で詰める余地がない**。オピニオンはプラットフォームの空気に引っ張られるので、自分のドメインに置きたい。加えて、静的サイト / CDN / CI を自分で組むこと自体が目的（SRE の練習台）であり、日英で書く場を持つことが英語学習のアウトプットになる。

## 自分の使い方
技術や働き方について考えがまとまったとき、日本語でオピニオンを書いて push する。同じ内容を英語で書き直す回では、英語学習のアウトプットとして使う。

## 課題
- 既存サービスは「速くしたい」という要求に応えられない（テーマも配信も他人の持ち物）。
- 日本語圏サービスと英語圏サービスが分断されていて、日英を同じ場所に置けない。

## 需要
**自分ひとりで十分**。ただし軽量サイト志向のコミュニティは実在し、外部の判定基準として使える。
- [512KB Club](https://512kb.club/) — 非圧縮 100KB 未満が green team。Cloudflare URL Scan の結果で申請する
- [250kb Club](https://250kb.club/) — 圧縮後サイズで判定（[HN スレ](https://news.ycombinator.com/item?id=25176663)）

## 競合
| | 速度 | それでも作る理由 |
|---|---|---|
| [Bear Blog](https://bearblog.dev/) | JS ゼロ・PSI 100（$5/mo） | ホスト型なので配信層を自分で触れない。**速度の限界を決めるのが他人**になる |
| Hugo / Astro のブログテーマ | 十分速い | 既定で不要なものが載る。削るより最初から作らないほうが速い |
| note / Zenn / はてな | 遅い | ドメインも配信も自分のものにならない |

**自分の版の違い**: 速度を「気をつける」ではなく **CI の合否条件**にする。予算を超えたらデプロイしない。

## 解決策
Markdown を git push すると静的 HTML が生成され、エッジから配信される。JS は 1 バイトも配信しない。CSS はインラインで、記事ページのリクエストは HTML 1 本だけ。CI が転送量バジェットと Lighthouse を測り、超えたら fail する。

## 速度目標（v1）
記事ページ 1 枚について、CI で自動判定する。**この 3 つが「予算」**で、超えたらデプロイしない:
- **実行される JS 0 バイト**（外部リクエスト 0 本）。唯一の例外は宣言的な `<script type="speculationrules">`（JSON。ブラウザが読むだけでコードは走らない）
- **転送量 ≤ 15KB**（brotli 圧縮後。brotli = 配信時に自動でかかる圧縮で、実際に回線を流れるのはこのサイズ。HTML 1 本のみ、フォント / 画像なし）
- **Lighthouse 100 / 100 / 100 / 100**（Performance / Accessibility / Best Practices / SEO）

## スコープ外
コメント / 検索 / アクセス解析 / タグ・カテゴリ / ページネーション / OGP 画像 / Web フォント / AI 翻訳 / CMS・管理画面 / 記事内の画像 / ダークモード切替ボタン（`prefers-color-scheme` のみ）。

## 完成の定義
**Markdown を 1 本 push したら記事ページが公開され、速度目標 3 つすべてを CI が通す。**

---
言語方針: 記事ごとに `ja` / `en` を持ち、対訳がある記事だけ相互リンクを出す（両言語必須にはしない）。
残す機能: RSS/Atom フィード、ダークモード（CSS のみ）。
